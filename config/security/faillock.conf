# Lock account after failed login attempts
# These match the Arch defaults but are set explicitly so they survive updates

# Lock after 3 failed attempts
deny = 3

# Within a 15-minute window
fail_interval = 900

# Unlock after 10 minutes
unlock_time = 600
