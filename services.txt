# System services (enabled via systemctl enable)
NetworkManager
bluetooth
greetd
nftables
sshd
pcscd

# User services (enabled via systemctl --user enable)
# PipeWire is socket-activated by default on modern Arch via presets.
# No need to enable explicitly.
